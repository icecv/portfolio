export interface BlogPost {
  id: string
  slug: string
  title: string
  description: string
  date: string
  tags: string[]
  readingTime: string
  content: string
}

export const blogPosts: BlogPost[] = [
  {
    id: '1',
    slug: 'multi-agent-system-design',
    title: '我如何设计一套真正能上生产的多智能体系统',
    description:
      '编排器-专家模式、智能体契约，以及将第一套 Agentic 系统交付给真实用户过程中学到的血泪教训。',
    date: '2024-04-10',
    tags: ['多智能体', 'LangChain', '系统设计', '生产实践'],
    readingTime: '8 分钟',
    content: `
## 为什么大多数多智能体 Demo 一到生产就崩

我见过很多多智能体 Demo，看起来令人惊叹：一群智能体自主地浏览网页、写代码、跑测试、部署上线。然后你尝试构建真实的东西，整个系统土崩瓦解。

问题不在于底层的大模型，而在于系统设计。

## 核心问题：智能体需要契约

构建微服务时，你会定义服务间严格的 API 契约。智能体系统同样需要这种约束——但大多数教程完全跳过了这一步。

**智能体契约**需要规定：
- 智能体接收什么输入（Schema，而非自由文本）
- 智能体负责什么（单一且明确的职责）
- 智能体产出什么输出（结构化的、可解析的 JSON）
- 智能体无法完成任务时做什么

没有契约，智能体就开始做不可预测的事：幻觉中间状态、返回没有任何东西能解析的格式、链路中的错误静默传播，直到你得到一个彻底损坏的最终输出。

## 真正有效的模式：编排器 + 专家

在三次失败的"自主"智能体循环实验之后，我找到了一个简单且有效的模式：

\`\`\`
用户查询
    ↓
[编排器]  ← 规划、协调、决策
    ↓           ↓           ↓
[智能体 A]  [智能体 B]  [智能体 C]  ← 专家，职责单一
    ↓           ↓           ↓
[聚合器] ← 合并结果，生成最终输出
\`\`\`

编排器的职责**仅限于规划和协调**——它本身不做实质性工作。专家智能体各司其职。聚合器负责合并与后处理。

## 让智能体可靠的四个原则

**1. 全程使用结构化输出。**
不要让智能体返回自由文本再用正则解析。使用 OpenAI Function Calling 或 Claude Tool Use 强制 JSON Schema。这能消除一整类 Bug。

**2. 在每个边界处做校验。**
每当一个智能体的输出成为下一个智能体的输入时，验证 Schema。大声失败，而非静默传播垃圾数据。

**3. 为局部失败设计。**
一个智能体失败不应该崩溃整个管道。构建重试逻辑、降级方案和优雅的容错处理。问自己：如果智能体 B 返回 null，智能体 C 和 D 该怎么做？

**4. 记录一切，什么都不要信。**
在我的系统里，每一次智能体调用都有日志：输入、输出、延迟、Token 用量。这是排查问题的前提，没有商量余地。智能体会做出令人意外的事——你需要完整的审计链路。

## Prompt 的设计原则

每个专家智能体应该有：
- 简短的系统提示（< 300 Token），只描述一件事
- 明确的输出格式规范
- "无法完成任务时该怎么做"的指令

编排器的提示词是最重要、也最难写好的。它需要：
1. 理解整体目标
2. 将其拆解为子任务
3. 将子任务分配给合适的专家
4. 指定期望接收回的输出格式

实践经验：让编排器在规划前先"逐步思考你有哪些信息、缺哪些信息"，能显著提升拆解质量。

## 总结与反思

- **严格的契约 > 聪明的智能体。** 有明确 Schema 约束的智能体，远胜于提示词模糊的"自主"智能体。
- **并行是双刃剑。** 并行运行智能体对延迟很好，对调试很差。并行化之前先加关联 ID 和集中日志。
- **编排器提示词是重中之重。** 把 80% 的 Prompt 工程时间花在这里——所有下游都依赖于它能正确规划。
- **先设计失败路径。** 智能体超时怎么办？API 挂了怎么办？检索结果为空怎么办？显式处理这些情况，不要让它们级联传播。

构建 Agentic 系统，软件工程的成分远大于 AI 工程。模型已经足够强大，你的工作是给它们正确的结构去发挥。
    `,
  },
  {
    id: '2',
    slug: 'prompt-engineering-patterns',
    title: 'Prompt 工程模式：超越"你是一个有帮助的助手"',
    description:
      '思维链、少样本示例、结构化输出、接地规则——这些在生产 LLM 应用中真正起效的具体模式。',
    date: '2024-03-15',
    tags: ['Prompt 工程', 'LLM', '生产实践', '最佳实践'],
    readingTime: '6 分钟',
    content: `
## Prompt 工程就是软件工程

第一次听到"Prompt 工程"时，我以为这不过是给 ChatGPT 写指令的花式叫法。经过两年构建生产级 LLM 系统，我现在认为它更准确的描述是：**对一个随机函数编程**。

输入是自然语言，输出是概率性的。但底层的工程纪律——清晰的规范、边界情况处理、测试——是纯粹的软件工程。

以下是我在每个生产系统中都在用的模式。

## 模式一：角色 + 上下文 + 任务 + 格式

最基础的提示词结构。每个生产提示词都应该包含这四个要素：

\`\`\`
[角色]
你是一位专注于 SaaS 指标分析的资深数据分析师。

[上下文]
用户正在查看一家 B2B 软件公司的月度营收数据。
可用指标：ARR、流失率、扩展营收、客户数量。

[任务]
分析所提供的指标，识别前 3 大业务风险。

[格式]
以 JSON 格式回答，包含 risks 数组，每项包含：
title（字符串）、severity（low|medium|high）、explanation（字符串，不超过 100 字）
\`\`\`

将这四个关注点分离，让提示词迭代更容易定位问题：输出不对时，可以判断是哪个部分出了问题。

## 模式二：思维链（CoT）处理复杂推理

对于任何需要多步推理的任务，让模型在回答前先思考：

\`\`\`
在给出最终答案之前，请逐步思考：
1. 我有哪些信息？
2. 缺少哪些信息？
3. 有哪些可能的处理方式？
4. 考虑约束条件，哪种方式最合适？

然后按指定格式给出你的最终答案。
\`\`\`

这在以下任务上能稳定提升输出质量：有边界情况的分类、代码生成、数学推理和规划。

## 模式三：接地规则（RAG 系统必备）

如果你的系统使用了检索，必须加入明确的接地规则：

\`\`\`
关键规则：只能基于下方 <context> 中的信息作答。
如果上下文信息不足以回答问题：
- 说"根据现有文档，我没有足够的信息来回答这个问题。"
- 不要推测，不要使用上下文以外的知识。
- 不要只说"我不知道"——请说明具体缺少什么信息会有帮助。
\`\`\`

这一条规则能预防 RAG 系统中绝大多数的幻觉问题。

## 模式四：结构化输出 + 校验

能用结构化输出就绝对不要解析自由文本：

\`\`\`python
from openai import OpenAI
from pydantic import BaseModel

class EvaluationResult(BaseModel):
    score: int  # 0-10
    reasoning: str
    recommendation: str

response = client.beta.chat.completions.parse(
    model="gpt-4o",
    messages=[...],
    response_format=EvaluationResult,
)
result = response.choices[0].message.parsed
\`\`\`

这能消除 JSON 解析错误，让整个管道端到端类型安全。

## 模式五：少样本示例保持一致性

需要输出风格保持一致时，3-5 个示例的效果远胜于详细说明：

\`\`\`
以下是分析格式的示例：

输入："营收增长 15%，但流失率上升至 8%"
输出：{"sentiment": "mixed", "key_risk": "用户留存", "priority": "high"}

输入："NPS 从 32 提升至 45，扩展营收增长 22%"
输出：{"sentiment": "positive", "key_risk": "无", "priority": "low"}

现在分析这个输入：{user_input}
\`\`\`

## 模式六：防御性提示

始终明确边界情况该怎么处理：

- 输入模糊时怎么做
- 超出范围的请求怎么处理
- 信息不足时怎么处理
- 无法完成任务时返回什么

如果你不指定，模型会自由发挥——而生产环境中的自由发挥是危险的。

## 测试你的提示词

没有测试的 Prompt 工程是在瞎蒙。我为每个提示词维护一个包含 20-30 个输入的测试集，涵盖：
- 正常路径（直接输入）
- 边界情况（异常或模糊输入）
- 对抗输入（设计用来干扰模型的输入）
- 回归用例（曾经失败的输入）

每次重大提示词变更都跑一遍评估。用 LLM-as-Judge 评估质量指标，用精确匹配校验结构。

模型已经非常强大。你的工作是给它们正确的结构，让这种能力可靠地释放。
    `,
  },
  {
    id: '3',
    slug: 'developer-to-ai-product',
    title: '从全栈开发者到 AI 产品构建者：真正改变了什么',
    description:
      '当你从构建传统软件转向构建 AI 原生产品时，真正重要的心智模型、技能和思维方式的转变。',
    date: '2024-02-20',
    tags: ['职业发展', 'AI 产品', '思考与反思', '工程'],
    readingTime: '5 分钟',
    content: `
## 没有人告诉你的那段转型期

两年前，我是一个知道怎么调用 OpenAI API 的全栈开发者。今天，我大部分时间都在设计 Agentic 系统、思考检索质量、调试概率性行为。

技术技能大部分迁移过来了。心智模型大部分没有。

## 真正不同的是什么

### 确定性 → 概率性

传统软件是确定性的。相同输入，相同输出。你可以写单测，复现 Bug，精确推理行为。

AI 系统是概率性的。相同输入可能产生不同输出。"测试"意味着构建评估集和度量分布，而不是断言相等。"调试"意味着理解失败的**模式**，而不是追踪执行路径。

这种转变会打破很多你原本没意识到自己持有的假设。

### 功能 → 能力 + 约束

在传统软件中，你构建功能。在 AI 产品中，你释放能力——然后把大部分时间花在**约束**这些能力上。

模型几乎什么都能做。你的工作是定义它在你的具体场景下应该**可靠地**做哪件事。这很反直觉：你拥有一个极其强大的通用系统，你的价值主张是让它更加受限。

### 正确性 → 足够好的质量

传统软件要么正常，要么不正常。AI 输出存在质量谱系。"足够好"是合理的标准，而知道标准该设在哪里是产品判断，不是技术判断。

90% 的准确率什么时候可以接受？什么时候需要 99%？这取决于用户体验、错误代价，以及是否有人工在回路中。这些不是工程问题——是产品问题。

## 比你预期更重要的技能

**清晰写作。** 你的提示词是规格说明。模糊的规格说明产生模糊的输出。你写精确、无歧义指令的能力越强，你的系统行为就越好。

**评估设计。** 构建好的测试集——能测试用户行为真实分布的输入——是核心技能。大多数团队跳过这步，然后为此付出代价。

**用户研究。** AI 系统常常在需求层面失败：你构建了错误的东西，或者衡量了错误的指标。在 AI 领域，构建前后都和用户深入交流比以往更重要，因为失败模式更加隐蔽。

**产品感觉。** 当 AI 出错时，是大问题还是小麻烦？系统应该大声失败还是优雅降级？这些需要很强的产品判断力。

## 比你预期迁移得更好的技能

**系统思维。** 设计智能体管道从根本上就是设计系统：组件、接口、数据流、失败模式。好的软件架构直觉可以直接迁移。

**调试纪律。** 当 AI 系统神秘地失败时，工程学的方法——隔离变量、形成假说、系统性验证——完全正确。

**API 设计直觉。** 你的提示词就是 API。同样的原则适用：清晰的契约、处理边界情况、谨慎地版本化。

## 最重要的思维方式

在 AI 领域成功的开发者，停止问"我怎么让这个能用？"，开始问"**我怎么让这个值得信赖？**"

值得信赖意味着：可预测的失败模式、良好的不确定性沟通、可审计性，以及优雅的降级。这些特性比原始能力更难构建，也正是区分 Demo 和产品的关键所在。

这项技术确实令人兴奋。但真正困难、真正重要的工作，是让它可靠到足以让人真正依赖。
    `,
  },
]

export function getBlogPostBySlug(slug: string): BlogPost | undefined {
  return blogPosts.find((p) => p.slug === slug)
}

export function getAllBlogPosts(): BlogPost[] {
  return [...blogPosts].sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())
}
